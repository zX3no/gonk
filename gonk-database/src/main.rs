#![allow(unused)]
use std::time::Instant;

use gonk_database::*;

fn main() {
    init();

    // let test = artists_albums_and_songs();
    // let (artists, albums, songs) = artists_albums_and_songs();
    // dbg!(albums);
    // bench_super_slow(|| {
    //     let (artits, albums, songs) = artists_albums_and_songs();
    // });
}
